# ==========================
# COMMON (réutilisable)
# ==========================
NODE_ENV=development

# URL du backend vue par le FRONT (adapter au contexte dev/prod)
API_BASE_URL=http://localhost:4000

# ==========================
# SERVER (copier dans server/.env)
# ==========================
PORT=4000
DATABASE_URL=postgres://user:password@localhost:5432/appdb
JWT_SECRET=change-me-in-prod
REFRESH_TOKEN_SECRET=change-me-too
CORS_ORIGIN=http://localhost:5173  # Vite
# CORS_ORIGIN=http://localhost:3000  # Next
REDIS_URL=redis://localhost:6379

# Email (optionnel)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Support <support@example.com>"

# Tiers (optionnel)
STRIPE_SECRET_KEY=
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
OPENAI_API_KEY=

# ==========================
# CLIENT (Vite) -> variables exposées DOIVENT commencer par VITE_
# Copier dans client/.env
# ==========================
VITE_API_BASE_URL=http://localhost:4000
VITE_PUBLIC_SITE_NAME=Mon SaaS
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_STRIPE_PUBLIC_KEY=
# Ajouter ici d'autres VITE_*

# ==========================
# CLIENT (Next.js) -> variables exposées DOIVENT commencer par NEXT_PUBLIC_
# (si tu utilises Next au lieu de Vite)
# ==========================
# NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
# NEXT_PUBLIC_SITE_NAME=Mon SaaS
# NEXT_PUBLIC_STRIPE_PUBLIC_KEY=
